<%= if users.any? %>
  <ul>
    <% users.each do |user| %>
      <li>
        <div>
          <%= user.name %>
        </div>
        <div>
          <P><%= link_to 'View profile', user_path(user) %></P>
        </div>
      </li>
    <% end %>
  </ul>
  <%= paginate users %>
<% end %>